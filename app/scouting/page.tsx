import { ScoutingPortfolio } from "@/components/ScoutingPortfolio";

export const metadata = {
  title: "Baseball Scouting Portfolio | Player Evaluations",
  description: "A scouting analyst portfolio for player evaluations, tool grades, and supporting baseball visuals."
};

export default function ScoutingPage() {
  return <ScoutingPortfolio />;
}
